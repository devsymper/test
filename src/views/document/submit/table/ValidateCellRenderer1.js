import BaseCellRenderer from "./BaseCellRenderer";

export default class ValidateCellRenderer1 extends BaseCellRenderer{
    init(params){
        super(params);
    }
    destroy(params) {
        
    }
}