import NodeBase from './NodeBase'

export default class UnionData extends NodeBase {
    constructor(symperId){
        let defaultConfig = {
            
        };
        super(symperId, 'Union', defaultConfig);
    }
}