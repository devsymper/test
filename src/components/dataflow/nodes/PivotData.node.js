import NodeBase from './NodeBase'

export default class PivotData extends NodeBase {
    constructor(symperId){
        let defaultConfig = {
            
        };
        super(symperId, 'Pivot', defaultConfig);
    }
}