import NodeBase from './NodeBase'

export default class SortData extends NodeBase {
    constructor(symperId){
        let defaultConfig = {
            
        };
        super(symperId, 'Sort', defaultConfig);
    }
}