import NodeBase from './NodeBase'

export default class SummarizeData extends NodeBase {
    constructor(symperId){
        let defaultConfig = {
            
        };
        super(symperId, 'Summarize', defaultConfig);
    }
}