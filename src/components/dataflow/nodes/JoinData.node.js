import NodeBase from './NodeBase'

export default class JoinData extends NodeBase {
    constructor(symperId){
        let defaultConfig = {
            
        };
        super(symperId, 'Join', defaultConfig);
    }
}